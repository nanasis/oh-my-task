{
  "name": "oh-my-task",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "bin": {
    "oh-my-task": "./scripts/install-skills.mjs"
  },
  "keywords": ["pi-package", "agent-skill", "task-management"],
  "pi": {
    "extensions": ["./packages/pi-extension/src/index.ts"],
    "skills": ["./skills"]
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspace=oh-my-task-cli && npm run build --workspace=oh-my-task-pi",
    "test": "npm run test --workspace=oh-my-task-cli && npm run test --workspace=oh-my-task-pi",
    "typecheck": "npm run build --workspace=oh-my-task-cli && npm run typecheck --workspace=oh-my-task-pi",
    "validate": "npm run typecheck && npm test && npm run build",
    "test:local": "node scripts/test-with-node-types.mjs",
    "install-skill": "node scripts/install-skills.mjs",
    "site:build": "python scripts/generate_design_html.py && python scripts/generate_project_site.py --output _site",
    "site:check": "python scripts/check_project_site.py _site"
  }
}
